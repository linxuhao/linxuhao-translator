{# 文件名: /app/thinking.jinja #}
{# 修复说明：去除了 enable_thinking 状态下预填充的 <think> 标签，将生成权完全交还给模型，确保 vLLM 能够监听到 decode 阶段的 think_start_str 从而激活 budget 计数器。 #}

{# ========================================== #}
{# 架构定位: 特洛伊木马嗅探模板 (绕过 vLLM API 丢包限制) #}
{# ========================================== #}

{# 🎯 物理嗅探：默认开启思考。如果系统指令里藏了暗号，立刻切断思考神经！ #}
{%- set enable_thinking = true %}
{%- if messages[0].role == 'system' and '<<DISABLE_THINKING>>' in messages[0].content %}
    {%- set enable_thinking = false %}
{%- endif %}

{%- if messages[0].role == 'system' %}
    {# 🎯 销毁证据：渲染时把暗号抹除，绝不让大模型看见 #}
    {%- set clean_sys = messages[0].content | replace('<<DISABLE_THINKING>>', '') %}
    {{- '<|im_start|>system\n' + clean_sys + '<|im_end|>\n' }}
{%- endif %}

{%- for message in messages %}
    {%- if message.role == 'user' or message.role == 'assistant' %}
        {# 强制清理历史残骸 #}
        {%- set clean_content = message.content | replace('<think>', '') | replace('</think>', '') %}
        {{- '<|im_start|>' + message.role + '\n' + clean_content + '<|im_end|>\n' }}
    {%- endif %}
{%- endfor %}

{# 🎯 终极引擎：执行劫持 #}
{%- if add_generation_prompt %}
    {%- if enable_thinking %}
        {# 外教专线 (修复点)：绝不能在此处硬编码 <think>，必须仅提供起手式，让模型自行 Decode <think> 以触发 vLLM 的 budget 计数器 #}
        {{- '<|im_start|>assistant\n' }}
    {%- else %}
        {# 翻译专线：检测到暗号，强制预填充闭合标签，毫秒直出！ #}
        {{- '<|im_start|>assistant\n<think>\n推理限额已达，我将直接输出确定性答案。</think>\n' }}
    {%- endif %}
{%- endif %}